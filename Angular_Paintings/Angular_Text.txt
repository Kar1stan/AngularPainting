Архитектура Angular из трех частей:базы данных,сервиса(получает и интерпретирует данные с базы данных и коспонента
(HTML,CSS-шаблоны) которые собираеться в корневой модуль который все это связывает AppModules написан Angular на 
Typescript котрый сам в свою очередь дополняет JS.
Что касается плюсов и минусов,насколько я понял(я еще не настолько знаю оба языка что бы вообще об этом говорить но задание такое) что Angular хуже по гибкости чем React хотя они использует один и тот же компонетный подход так же проблема с переходом на новые версии и трудный?(Ну React легче писать) язык программированя.Плюсы:получения данных напрямую,обширный выбор функций(полагаю из-за Typescript),маленькая вероятность 
сбоев и ошибок?(без понятия  о чем это).